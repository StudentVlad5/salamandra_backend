const getMenu = require('./getAll');
const getMenuById = require('./getId');
const updateMenu = require('./updateMenu');
const deleteMenu = require('./delete');
const createMenu = require('./createMenu');

module.exports = {
  getMenu,
  getMenuById,
  updateMenu,
  deleteMenu,
  createMenu,
};
